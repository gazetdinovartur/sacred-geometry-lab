knpu_oauth2_client:
    clients:
        google:
            type: google
            client_id: '%env(default::OAUTH_GOOGLE_ID)%'
            client_secret: '%env(default::OAUTH_GOOGLE_SECRET)%'
            redirect_route: auth_google_callback
